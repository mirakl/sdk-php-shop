# (RT29) Mark a return as canceled

```php
use Mirakl\MMP\Common\Domain\Returns\CancelReturns\Returns;
use Mirakl\MMP\Shop\Client\ShopApiClient;
use Mirakl\MMP\Shop\Request\Returns\CancelReturnsRequest;

$api = new ShopApiClient('https://your-marketplace.mirakl.net/api', 'your-api-key', 'your-shop-id');

$return = new Returns();
$return->setId('return-001');

$request = new CancelReturnsRequest();
$request->setReturns([$return]);

$result = $api->cancelReturns($request);
// $result => @see \Mirakl\MMP\Common\Domain\Returns\Returns

$errors = $result->getReturnErrors();
$successes = $result->getReturnSuccess();
```
